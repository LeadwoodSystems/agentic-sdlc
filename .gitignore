# Secrets — never commit real values; use .env.example templates instead.
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
id_rsa
id_ed25519
credentials.json
service-account*.json
secrets/

# Subagent-driven-development scratch: per-plan ledgers, task briefs, review
# packages. Recovery state for a single session, superseded by git history.
.superpowers/

# OS / editor cruft
.DS_Store
Thumbs.db
*.code-workspace
